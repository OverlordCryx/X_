local HttpService = game:GetService("HttpService")
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local player = Players.LocalPlayer
local WebhookURL = "https://discord.com/api/webhooks/1470105884360310947/iPUC11NmqQRAwJilsE-i6k8y5ZQzlqhECwsKP_9Js4itjQRHF1mCCPToY-hf9oo5asEy"
local device = "Unknown"
if UserInputService.TouchEnabled and not UserInputService.KeyboardEnabled then
	device = "Mobile"
elseif UserInputService.KeyboardEnabled then
	device = "PC"
elseif UserInputService.GamepadEnabled then
	device = "Console"
end
local profileLink = "https://www.roblox.com/users/" .. player.UserId .. "/profile"
local joinLink = "https://www.roblox.com/games/" .. game.PlaceId .. "?gameInstanceId=" .. game.JobId
local data = {
	username = "NOTHING_X",
	embeds = {{
		title = "NOTHING X (UN)",
		color = 16753920,
		fields = {
			{
				name = "Username",
				value = player.Name,
				inline = true
			},
			{
				name = "User ID",
				value = tostring(player.UserId),
				inline = true
			},
			{
				name = "Server Join",
				value = "[JOIN SERVER](" .. joinLink .. ")",
				inline = false
			},
			{
				name = "Profile Player",
				value = "[VIEW PROFILE](" .. profileLink .. ")",
				inline = false
			},
			{
				name = "Server ID",
				value = tostring(game.JobId),
				inline = true
			},
			{
				name = "Device",
				value = device,
				inline = true
			}
		},
		footer = {
			text = "NOTHING X Logger"
		},
		timestamp = os.date("!%Y-%m-%dT%H:%M:%SZ")
	}}
}
local requestFunc =
	syn and syn.request or
	http_request or
	request or
	(fluxus and fluxus.request)
pcall(function()
	if requestFunc then
		requestFunc({
			Url = WebhookURL,
			Method = "POST",
			Headers = {
				["Content-Type"] = "application/json"
			},
			Body = HttpService:JSONEncode(data)
		})
	end
end)
